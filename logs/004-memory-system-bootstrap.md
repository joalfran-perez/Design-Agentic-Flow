# Log 004 — Persistent memory system bootstrap

**Date:** 2026-08-27

**What:** Installed the full persistent-memory structure per user request: `AGENTS.md`, `context/`
(design.md + decisiones.md), `decisions/` (5 retroactive ADRs), `state/` (current.md + inventories.md),
`skills/` (2 procedures), `gotchas/` (6 known-issue files), `logs/` (this + 3 retroactive session
summaries covering logs 001-003).

**Source of content:** the 3 completed inventory folders' README/tokens/components files, plus the prior
session transcript's decisions and error-recovery patterns (all already summarized in this conversation's
context before this task started) — no new Figma extraction was performed this session.

**Repo state at time of bootstrap:** git repo with no commits yet, no remote, 3 inventory folders
untracked. Memory files added on top, not yet committed (commit is the user's call, not done
automatically per the git-safety rules).

**No open technical work** — see `state/current.md` for the 4 open questions surfaced for the user.
